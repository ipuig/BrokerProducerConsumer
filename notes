

Header (8 Bytes)

 1b    1b     3b      1b      2b
[PT|Plength|NodeId|StreamId|Plabel] + [Payload]


Packet types:

    - PUBLISH 10 (Producer->Broker)
    - PUBLISH ACK 110 (Broker->Producer)

    - LIST REQUEST 11 (Consumer->Broker)
    - LIST REQUEST ACK 111 (Broker->Consumer)
    
    - LIST DATA 12 (Broker->Consumer)
    - LIST DATA ACK 112 (Consumer->Broker)

    - SUBSCRIBE 13 (Consumer->Broker)
    - SUBSCRIBE ACK 113 (Broker->Consumer)

    - UNSUBSCRIBE 14 (Consumer->Broker)
    - UNSUBSCRIBE ACK 114 (Broker->Consumer)

    - FORWARD 15 (Broker->Consumer)
    - FORWARD ACK 115 (Consumer->Broker)

Payload length: size of the payload in bytes

NodeId (3 Bytes)

   1b           2b
[NodeType][Last ip octet]

Node types:

    - Broker   0xA
    - Consumer 0xC
    - Producer 0xE

StreamId (1 Byte)

Stream types:
    - 0 is not a stream
    - otherwise stream number

PLabel 

  1b     1b
[PType][PTime]
        

Payload types:

    - audio file 0xAF
    - frame file 0xFF
    - Nothing 0

Payload times:
    - the byte number represents the timestamp
    
